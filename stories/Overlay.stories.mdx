import { boolean, number} from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Icon from '../src/atoms/Icon';
import Overlay from '../src/atoms/Overlay';

<Meta title="Atoms|Overlay" component={Overlay} />

## Props

<Props of={Overlay} />

## Preview

<Preview>
  <Story name="Overlay">
    <Overlay
      topPosition={boolean('top Position', false)}
      zIndex={number('Z index', 901)}
      overlayOpacity={number('Overlay opacity', 0.7)}
      onClick={action('onClick')}
    >
    <Icon size="XL" type="guarantee" />
    </Overlay>
  </Story>
</Preview>
