import { text, select, boolean, number } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import ListItem from '../src/molecules/ListItem';

<Meta title="ListItem" component={ListItem} />

## Preview

<Preview>
  <Story name="ListItem">
    <ListItem
      type={select(
        'type',
        {
          Normal: 'normal',
          Accent: 'accent',
          Main: 'main',
        },
        'normal'
      )}
      text={text('Text', 'Cargo por servicio')}
      price={number('Price', 80)}
      currency={text('Currency', 'MXN')}
      hasNotice={boolean('Has notice', false)}
    />
  </Story>
</Preview>

## Props

<Props of={ListItem} />
