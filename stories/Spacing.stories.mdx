import { text, boolean, select, number } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import Spacing from '../src/atoms/Spacing';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Text from '../src/atoms/Text';
import Rating from '../src/molecules/Rating';
import Input from '../src/molecules/Input';
import Select from '../src/molecules/Select';

<Meta title="Spacing|Spacing" component={Spacing} />

# Spacing component

<Preview>
  <Story name="Spacing Story">
    <Spacing
      vertical={boolean('Vertical', false)}
        size={select(
          'Size',
          {
            XSmall: 'XS',
            Small: 'S',
            Medium: 'M',
            Large: 'L',
            XLarge: 'XL',
          },
          'L'
        )}
        responsiveSize={select(
          'Responsive Size',
          {
            Empty: '',
            XSmall: 'XS',
            Small: 'S',
            Medium: 'M',
            Large: 'L',
            XLarge: 'XL',
          },
          ''
        )}
        flexGrow={boolean('Flex Grow', false)}
        alignItems={select(
          'Align Items',
          {
            Initial: 'initial',
            Stretch: 'stretch',
            FlexStart: 'flex-start',
            FlexEnd: 'flex-end',
            Center: 'center',
            Baseline: 'baseline',
          },
          'initial'
        )}
        justifyContent={select(
          'Justify Content',
          {
            Initial: 'initial',
            FlexStart: 'flex-start',
            FlexEnd: 'flex-end',
            Center: 'center',
            SpaceBetween: 'space-between',
            SpaceArround: 'space-arround',
          },
          'initial'
        )}
        isResponsive={boolean('Is responsive', false)}
        responsiveScreen={select(
          'Responsive screen',
          {
            Small: 'small',
            Medium: 'medium',
          },
          'small'
        )}
        responsiveColumnReverse={boolean('Column reverse', false)}
        textAlign={select(
          'Text align',
          {
            Left: 'left',
            Center: 'center',
            Right: 'right',
          },
          'left'
        )}
      >
      <Text size="M" weight="semibold">First component</Text>
      <Text size="M" weight="semibold">Second text component</Text>
      <Text size="M" weight="semibold">Third text component</Text>
    </Spacing>
  </Story>
</Preview>

## Props

<Props of={Spacing} />

## Margin horizontal example

<Preview>
  <Story name="Margin horizontal example">
    <Spacing size="S">
      <Text size="M" weight="semibold">Lisboa</Text>
      <Rating rating={3.5} compact />
    </Spacing>
    </Story>
</Preview>

## Margin vertical example

<Preview>
  <Story name="Margin vertical example">
    <Spacing vertical size="S">
      <Text size="XXL" weight="semibold">Pestana CR7 Lisboa</Text>
      <Text size="M" weight="semibold">PURE Lifestyle is our personality - A partnership between Pestana Hotel Group and Cristiano Ronaldo.</Text>
    </Spacing>
    </Story>
</Preview>

## Row example
With the `horizontal` direction and the `flex: 1` property, rows can be created with children of the same size and occupy 100% of the width.

<Preview>
  <Story name="Row example">
    <Spacing
        flexGrow
        isResponsive
      >
      <Input
        label="Nombre"
        id="name"
        value="Mary Elizabeth"
        onChange={action('onChange')}
      />
      <Input
        label="Apellido"
        id="lastname"
        value="Winstead"
        onChange={action('onChange')}
      />
    </Spacing>
  </Story>
</Preview>

## Spacing inside Spacing
You can use the Spacing component inside another Spacing component to create layouts.
The first Spacing has a vertical direction, the inner Spacing have a horizontal direction.

<Preview>
  <Story name="Spacing inside spacing">
    <Spacing vertical size="L">
      <Spacing
        flexGrow
        isResponsive
      >
      <Input
        label="Nombre"
        id="name"
        value="Mary Elizabeth"
        onChange={action('onChange')}
      />
      <Input
        label="Apellido"
        id="lastname"
        value="Winstead"
        onChange={action('onChange')}
      />
    </Spacing>
    <Spacing
        flexGrow
        isResponsive
      >
      <Input
        label="Nombre"
        id="name"
        value="Mary Elizabeth"
        onChange={action('onChange')}
      />
      <Input
        label="Apellido"
        id="lastname"
        value="Winstead"
        onChange={action('onChange')}
      />
      <Input
        label="Email"
        id="email"
        value="mew@email.io"
        onChange={action('onChange')}
        type="email"
      />
    </Spacing>
    </Spacing>
  </Story>
</Preview>

