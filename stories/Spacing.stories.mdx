import { text, boolean, select, number } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import Spacing from '../src/atoms/Spacing';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Input from '../src/molecules/Input';
import Select from '../src/molecules/Select';

<Meta title="Spacing|Spacing" component={Spacing} />

# Spacing component

<Preview>
  <Story name="Spacing Story">
    <Spacing
      direction={select(
          'Direction',
          {
            Both: 'both',
            Horizontal: 'horizontal',
            Vertical: 'vertical',
          },
          'horizontal'
        )}
        sizeHorizontal={select(
          'Size Horizontal',
          {
            Empty: '',
            XSmall: 'XS',
            Small: 'S',
            Medium: 'M',
            Large: 'L',
            XLarge: 'XL',
          },
          'M'
        )}
        sizeVertical={select(
          'Size Vertical',
          {
            Empty: '',
            XSmall: 'XS',
            Small: 'S',
            Medium: 'M',
            Large: 'L',
            XLarge: 'XL',
          },
          ''
        )}
        flex={text('Flex', '0 1 auto')}
        alignItems={select(
          'Align Items',
          {
            Initial: 'initial',
            Stretch: 'stretch',
            FlexStart: 'flex-start',
            FlexEnd: 'flex-end',
            Center: 'center',
          },
          'initial'
        )}
        justifyContent={select(
          'Justify Content',
          {
            Initial: 'initial',
            FlexStart: 'flex-start',
            FlexEnd: 'flex-end',
            Center: 'center',
            SpaceBetween: 'space-between',
            SpaceArround: 'space-arround',
          },
          'initial'
        )}
        isResponsive={boolean('Is responsive', false)}
        responsiveScreen={select(
          'Responsive screen',
          {
            Small: 'small',
            Medium: 'medium',
          },
          'small'
        )}
      >
      <Input
        label="Nombre"
        id="email"
        value=""
        onChange={action('onChange')}
      />
     <Select
      label="Nacionalidad"
      id="nacionalidad"
      placeholder={text('placeholder', 'Select Placeholder')}
      options={[
        {
          value: '1',
          label: 'Option 1',
        },
        {
          value: '2',
          label: 'Option 2',
        },
        {
          value: '3',
          label: 'Option 3',
        },
        ]}
        onChange
        value=""
      />
      <Input
        label="Apellido"
        id="email"
        value=""
        onChange={action('onChange')}
      />
    </Spacing>
  </Story>
</Preview>

## Props

<Props of={Spacing} />
