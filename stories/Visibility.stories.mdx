import { boolean, text, select } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Visibility from '../src/atoms/Visibility';

<Meta title="Atoms|Visibility" component={Visibility} />

## Preview

Use the default logo (Reservamos)

<Preview>
  <Story name="Visibility">
    <Visibility
      type={select(
        'type',
        {
          showForMobileOnly: 'showForMobileOnly',
          hideForMobileOnly: 'hideForMobileOnly',
          hideForLargeOnly: 'hideForLargeOnly',
          hideForMedium: 'hideForMedium',
        },
        'hideForMedium'
      )}
    >
      <p>Children</p>
    </Visibility>
  </Story>
</Preview>

## Props

<Props of={Visibility} />
