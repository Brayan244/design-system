import { useState } from 'react';
import { text, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import SmallSelect from '../src/molecules/SmallSelect';

<Meta title="Forms|SmallSelect" component={SmallSelect} />

## Preview

<Preview>
  <Story name="SmallSelect Story" height="200px">
    {() => {
      const [value, setValue] = useState('');
      return (
        <SmallSelect
          label={text('Label', 'Edades de los niños')}
          id={text('ID', 'nacionalidad')}
          placeholder={text('placeholder', 'Edad niño 01')}
          options={[
            {
              value: '1',
              label: 'Option 1',
            },
            {
              value: '2',
              label: 'Option 2',
            },
            {
              value: '3',
              label: 'Option 3',
            },
          ]}
          onChange={e => setValue(e.target.value)}
          value={value}
          isDisabled={boolean('Is disabled?', false)}
        />
      );
    }}
  </Story>
</Preview>

## Props

<Props of={SmallSelect} />
