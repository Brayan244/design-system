import { text, boolean, select } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Button from '../src/organisms/Button';

<Meta title="Organisms|Button" component={Button} />

## Preview

<Preview>
  <Story name="Button">
    <Button
      fullWidthOnSmall={boolean('Full Width On Small', false)}
      text={text('Text', 'Guardar')}
      onClick={action('onClick')}
      isDisabled={boolean('Is Disabled', false)}
      variant={select(
        'Variant',
        {
          Accent: 'accent',
          NoFill: 'noFill',
          Danger: 'danger',
          Discount: 'discount',
          Default: 'default',
        },
        'accent'
      )}
      isLoading={boolean('Is Loading', false)}
      fontSize={select('Font Size', ['XS', 'S', 'M', 'L', 'XL', 'XXL'], 'M')}
      padding={select('Padding', ['S', 'M', 'L'], 'L')}
      buttonType={select(
        'Button type',
        {
          Button: 'button',
          Submit: 'submit',
        },
        'button'
      )}
    />
  </Story>
</Preview>

## Props

<Props of={Button} />
