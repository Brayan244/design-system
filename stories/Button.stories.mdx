import { text, boolean, select } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Spacing from '../src/atoms/Spacing';
import Button from '../src/organisms/Button';
import LoaderButton from '../src/molecules/LoaderButton';
import backgroundColors from '../.storybook/backgroundColors';

<Meta title="Trigger|Button" component={Button} />

## Preview

<Preview>
  <Story name="Button">
    <Button
      withHeight={boolean('With Height', false)}
      fullWidthOnSmall={boolean('Full Width On Small', false)}
      text={text('Text', 'Guardar')}
      onClick={action('onClick')}
      isDisabled={boolean('Is Disabled', false)}
      variant={select(
        'Variant',
        {
          Accent: 'accent',
          NoFill: 'noFill',
          Danger: 'danger',
          Discount: 'discount',
          Default: 'default',
        },
        'accent'
      )}
      isLoading={boolean('Is Loading', false)}
      fontSize={select('Font Size', ['XS', 'S', 'M', 'L', 'XL', 'XXL'], 'M')}
      padding={select('Padding', ['S', 'M', 'L'], 'L')}
      buttonType={select(
        'Button type',
        {
          Button: 'button',
          Submit: 'submit',
        },
        'button'
      )}
    />
  </Story>
</Preview>

## isLoading
<Preview>
  <Story name="isLoading" parameters={{backgrounds: [...backgroundColors, { name: 'gray200', value: '#E6E6E6', default: true }]}}>
    <LoaderButton isNoFill={boolean('Is No Fill', false)} />
  </Story>
</Preview>

## All variants

<Preview>
  <Story name="All variants">
    <Spacing vertical>
      <Spacing>
        <Button text="Accent" onClick={action('onClick')} variant="accent" />
        <Button
          text="Accent"
          onClick={action('onClick')}
          variant="accent"
          isDisabled
        />
      </Spacing>
      <Spacing>
        <Button
          text="Discount"
          onClick={action('onClick')}
          variant="discount"
        />
        <Button
          text="Discount"
          onClick={action('onClick')}
          variant="discount"
          isDisabled
        />
      </Spacing>
      <Spacing>
        <Button text="noFill" onClick={action('onClick')} variant="noFill" />
        <Button
          text="noFill"
          onClick={action('onClick')}
          variant="noFill"
          isDisabled
        />
      </Spacing>
      <Spacing>
        <Button text="Danger" onClick={action('onClick')} variant="danger" />
        <Button
          text="Danger"
          onClick={action('onClick')}
          variant="danger"
          isDisabled
        />
      </Spacing>
      <Spacing>
        <Button text="Default" onClick={action('onClick')} variant="default" />
        <Button
          text="Default"
          onClick={action('onClick')}
          variant="default"
          isDisabled
        />
      </Spacing>
    </Spacing>
  </Story>
</Preview>

## Props

<Props of={Button} />
