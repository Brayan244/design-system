import { boolean, select } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import CardIcon from '../src/atoms/CardIcon';

<Meta title="CardIcon" component={CardIcon} />

## Props

<Props of={CardIcon} />

## Preview

<Preview>
  <Story name="CardIcon">
    <CardIcon
      cardBrand={select('type', ['visa', 'mastercard', 'amex'], 'default')}
      hasMargin={boolean('Has Margin', false)}
    />
  </Story>
</Preview>
