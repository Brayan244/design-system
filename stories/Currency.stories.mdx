import { text, number, select, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Currency from '../src/molecules/Currency';

<Meta title="Molecules|Currency" component={Currency} />

## Preview

<Preview>
  <Story name="Currency">
    <Currency
      price={number('Currency', 10200)}
      decimals={number('Decimals', 2)}
      currency={text('Currency', 'usd')}
      size={select('Size', ['XS', 'S', 'M', 'L', 'XL', 'XXL'], 'XXL')}
      color={select(
        'Color',
        [
          'primary',
          'accent',
          'grayStrong',
          'grayMedium',
          'grayLight',
          'info',
          'discount',
        ],
        'primary'
      )}
      weight={select('Weight', ['regular', 'semibold', 'bold'], 'bold')}
      lineThrough={boolean('Has line through?', false)}
    />
  </Story>
</Preview>

## Discount

<Preview>
  <Story name="Discount">
    <Currency
      price={2039}
      decimals={2}
      currency="mxn"
      size="XXL"
      color="discount"
      weight="bold"
    />
  </Story>
</Preview>

## Line Through

<Preview>
  <Story name="Line Through">
    <Currency
      price={2039}
      size="M"
      color="grayLight"
      weight="semibold"
      lineThrough
    />
  </Story>
</Preview>

## Props

<Props of={Currency} />
