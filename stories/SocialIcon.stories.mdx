import { select } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import SocialIcon from '../src/atoms/SocialIcon';

<Meta title="Social icon" component={SocialIcon} />

## Props

<Props of={SocialIcon} />

## Preview

<Preview>
  <Story name="Social Icon">
    <SocialIcon
      type={select(
        'type',
        {
          Instagram: 'instagram',
          Facebook: 'facebook',
          Twitter: 'twitter',
        },
        'instagram'
      )}
    />
  </Story>
</Preview>
