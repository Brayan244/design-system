import { select, text, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FlatButton from '../src/atoms/FlatButton';

<Meta title="Atoms|Flat Button" component={FlatButton} />

## Preview

<Preview>
  <Story name="FlatButton">
    <FlatButton
      isDisabled={boolean('Is disabled?', false)}
      onClick={action('onClick')}
      text={text('Text', 'Guardar')}
      type={select('type', ['accent', 'disabled', 'info', 'error'], 'accent')}
      size={select(
        'Size',
        {
          XSmall: 'XS',
          Small: 'S',
          Medium: 'M',
          Large: 'L',
          XLarge: 'XL',
          XXLarge: 'XXL',
        },
        'M'
      )}
    />
  </Story>
</Preview>

## Props

<Props of={FlatButton} />
