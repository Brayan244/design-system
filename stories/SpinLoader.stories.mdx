import { text, boolean } from '@storybook/addon-knobs';
import { select } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import SpinLoader from '../src/molecules/SpinLoader';

<Meta title="Loader|SpinLoader" component={SpinLoader} />

## Preview

<Preview>
  <Story name="SpinLoader">
    <SpinLoader
      text={text('Text', 'Loading...')}
      verticalMargin={boolean('Vertical Margin', false)}
      horizontalMargin={boolean('Horizontal Margin', false)}
    />
  </Story>
</Preview>

## Props

<Props of={SpinLoader} />
