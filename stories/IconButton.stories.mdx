import { text, boolean, select } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Spacing from '../src/atoms/Spacing';
import IconButton from '../src/atoms/IconButton';
import backgroundColors from '../.storybook/backgroundColors';

<Meta title="Buttons|Icon Button" component={IconButton} />

## Preview

<Preview>
  <Story name="Icon Button">
    <IconButton
      onClick={action('onClick')}
      isDisabled={boolean('Is Disabled', false)}
      variant={select(
        'Variant',
        {
          Accent: 'accent',
          NoFill: 'noFill',
          Primary: 'primary',
        },
        'accent'
      )}
      padding={select('Padding', ['S', 'M', 'L'], 'S')}
      buttonType={select(
        'Button type',
        {
          Button: 'button',
          Submit: 'submit',
        },
        'button'
      )}
      iconType={text('Icon Type', 'warning')}
      iconSize={select('Icon Size', ['S', 'M', 'L'], 'S')}
    />
  </Story>
</Preview>

## All variants

<Preview>
  <Story name="All variants">
    <Spacing vertical>
      <Spacing>
        <IconButton onClick={action('onClick')} variant="accent" />
        <IconButton onClick={action('onClick')} variant="accent" isDisabled />
      </Spacing>
      <Spacing>
        <IconButton
          iconType="plus"
          onClick={action('onClick')}
          variant="noFill"
        />
        <IconButton
          iconType="plus"
          onClick={action('onClick')}
          variant="noFill"
          isDisabled
        />
      </Spacing>
      <Spacing>
        <IconButton
          iconType="searchWhite"
          onClick={action('onClick')}
          variant="primary"
        />
        <IconButton
          iconType="searchWhite"
          onClick={action('onClick')}
          variant="primary"
          isDisabled
        />
      </Spacing>
    </Spacing>
  </Story>
</Preview>

## Props

<Props of={IconButton} />
