import { select, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import LoaderPulse from '../src/atoms/LoaderPulse';

<Meta title="Loader|Pulse" component={LoaderPulse} />

## Props

<Props of={LoaderPulse} />

## Preview

<Preview>
  <Story name="LoaderPulse">
    <LoaderPulse
      size={select(
        'Size',
        {
          Small: 'S',
          Medium: 'M',
        },
        'S'
      )}
      color={select('Color', ['primary', 'accent', 'gray', 'white'], 'gray')}
      hasWrapper={boolean('Has wrapper?', true)}
    />
  </Story>
</Preview>
