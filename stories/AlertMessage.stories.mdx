import { text, select, boolean } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import AlertMessage from '../src/molecules/AlertMessage';

<Meta title="Molecules|Alert Message" component={AlertMessage} />

## Props

<Props of={AlertMessage} />

## Preview

<Preview>
  <Story name="Alert Message">
    <AlertMessage
      text={text('Text', 'Alerta de precio activada correctamente')}
      title={text('Title', 'Alerta de precio ')}
      type={select(
        'type',
        {
          Success: 'success',
          Error: 'error',
          Warning: 'warning',
          Info: 'info',
        },
        'success'
      )}
    />
  </Story>
</Preview>
