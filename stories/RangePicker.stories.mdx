import { text, number, boolean, object } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import RangePicker from '../src/organisms/RangePicker';

<Meta title="Organisms|RangePicker" component={RangePicker} />

## RangePicker

<Preview>
  <Story name="RangePicker">
    <RangePicker
      label={text('Label', 'Categoría del hotel')}
      min={number('min', 2)}
      max={number('max', 4)}
      options={object('options', [1, 2, 3, 4, 5])}
      onOptionClick={action('onOptionClicked')}
    />
  </Story>
</Preview>

## Props

<Props of={RangePicker} />
