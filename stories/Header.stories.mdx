import { text } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Header from '../src/molecules/Header';

<Meta title="Header" component={Header} />

## Preview

<Preview>
  <Story name="Header">
    <Header
      alt={text('Alt', 'Logo')}
      text={text('Text', 'Mi cuenta')}
      url={text('URL', 'https://www.reservamos.mx/')}
    />
  </Story>
</Preview>

If no url prop is passed, the back button won't show up (small screens only)

<Preview>
  <Story name="Header without back button">
    <Header text="Sin botón" />
  </Story>
</Preview>

## Props

<Props of={Header} />
