import { text } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Header from '../src/molecules/Header';

<Meta title="Molecules|Header" component={Header} />

## Preview

<Preview>
  <Story name="Header">
    <Header
      alt={text('Alt', 'Logo')}
      title={text('Title', 'Mi cuenta')}
      url={text('URL', 'https://www.reservamos.mx/')}
      photoUrl={text(
        'Photo URL',
        'https://i.blogs.es/d9095a/lily-collins/650_1200.jpg'
      )}
    />
  </Story>
</Preview>

If no url prop is passed, the back button won't show up (small screens only)

<Preview>
  <Story name="Header without back button">
    <Header
      text="Sin botón"
      alt="logo"
      photoUrl="https://i.blogs.es/d9095a/lily-collins/650_1200.jpg"
    />
  </Story>
</Preview>

If no title prop is passed, the logo show up (small screens only)

<Preview>
  <Story name="Header without title">
    <Header
      alt="logo"
      photoUrl="https://i.blogs.es/d9095a/lily-collins/650_1200.jpg"
    />
  </Story>
</Preview>

## Props

<Props of={Header} />
