import { text, number, select, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Price from '../src/molecules/Price';

<Meta title="Molecules|Price" component={Price} />

## Preview

<Preview>
  <Story name="Price">
    <Price
      price={number('Price', 10200)}
      currency={text('Currency', 'usd')}
      size={select('Size', ['XS', 'S', 'M', 'L', 'XL', 'XXL'], 'M')}
      color={select(
        'Color',
        [
          'primary',
          'accent',
          'grayStrong',
          'grayMedium',
          'grayLight',
          'info',
          'discount',
        ],
        'grayStrong'
      )}
      weight={select('Weight', ['regular', 'semibold', 'bold'], 'regular')}
    />
  </Story>
</Preview>

## Props

<Props of={Price} />
