import { text, boolean, select } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import LoaderButton from '../src/molecules/LoaderButton';
import backgroundColors from '../.storybook/backgroundColors';

<Meta title="Molecules|Loader Button" component={LoaderButton} />

## Props

<Props of={LoaderButton} />

## Preview

<Preview>
  <Story name="LoaderButton" parameters={{backgrounds: [...backgroundColors, { name: 'gray200', value: '#E6E6E6', default: true }]}}>
    <LoaderButton isNoFill={boolean('Is No Fill', true)} />
  </Story>
</Preview>
