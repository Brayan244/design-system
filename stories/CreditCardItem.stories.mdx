import { text, select } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import CreditCardItem from '../src/molecules/CreditCardItem';

<Meta title="CreditCardItem" component={CreditCardItem} />

## Props

<Props of={CreditCardItem} />

## Preview

<Preview>
  <Story name="CreditCardItem">
    <CreditCardItem
      buttonText={text('Button text', 'Eliminar')}
      cardBrand={select('Card brand', ['visa', 'mastercard', 'amex'], 'default')}
      onDelete={action('onDelete')}
      text={text('Text', '**** **** **** 0182')}
    />
  </Story>
</Preview>
