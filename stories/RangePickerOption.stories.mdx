import { number, boolean } from '@storybook/addon-knobs';
import { action } from '@storybook/addon-actions';
import RangePickerOption from '../src/molecules/RangePickerOption';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

<Meta title="Molecules|RangePickerOption" component={RangePickerOption} />

## RangePickerOption

<Preview>
  <Story name="RangePickerOption">
    <RangePickerOption
      option={number('number', 1)}
      isActive={boolean('isActive?', false)}
      onClick={action('onClick')}
    />
  </Story>
</Preview>

## Props

<Props of={RangePickerOption} />
