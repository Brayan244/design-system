import { boolean, select } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import LoaderPhoto from '../src/atoms/LoaderPhoto';

<Meta title="Atoms|LoaderPhoto" component={LoaderPhoto} />

## Props

<Props of={LoaderPhoto} />

## Preview

<Preview>
  <Story name="LoaderPhoto">
    <LoaderPhoto
      type={select(
        'Type',
        {
          Large: 'L',
          Medium: 'M',
          Small: 'S',
        },
        'L'
      )}
      isLoader={boolean('Is Loader', true)}
    />
  </Story>
</Preview>
