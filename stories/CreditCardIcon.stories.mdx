import { boolean, select } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import CreditCardIcon from '../src/atoms/CreditCardIcon';

<Meta title="CreditCardIcon" component={CreditCardIcon} />

## Props

<Props of={CreditCardIcon} />

## Preview

<Preview>
  <Story name="CreditCardIcon">
    <CreditCardIcon
      cardBrand={select('type', ['visa', 'mastercard', 'amex'], 'default')}
      hasMargin={boolean('Has Margin', false)}
      hideBorder={boolean('Hide Border', false)}
    />
  </Story>
</Preview>
