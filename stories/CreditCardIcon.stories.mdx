import { boolean, select } from '@storybook/addon-knobs';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import CreditCardIcon from '../src/atoms/CreditCardIcon';
import backgroundColors from '../.storybook/backgroundColors';

<Meta title="Atoms|Credit Card Icon" component={CreditCardIcon} />

## Preview

<Preview>
  <Story name="CreditCardIcon" parameters={{backgrounds: [...backgroundColors, { name: 'gray200', value: '#E6E6E6', default: true }]}}>
    <CreditCardIcon
      cardBrand={select('type', ['visa', 'mastercard', 'amex'], 'default')}
      hasMargin={boolean('Has Margin', false)}
      hideBorder={boolean('Hide Border', false)}
    />
  </Story>
</Preview>

## Props

<Props of={CreditCardIcon} />
